---
name: Create Master Slug List on PR Merge
on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        id: setup_python
        with:
          python-version: '3.9'
      - name: cache virtualenv
        uses: actions/cache@v4
        id: cache-venv
        with:
          path: ${{ env.pythonLocation }}
          key: ${{ runner.os }}-${{ steps.setup_python.outputs.python-version }}-${{ env.pythonLocation }}-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-${{ steps.setup_python.outputs.python-version }}-${{ env.pythonLocation }}-
      - name: Install dependencies
        shell: bash
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
        if: steps.cache-venv.outputs.cache-hit != 'true'
      - name: Regenerate Master Slug List
        run: python3 tests/generate-slug-list.py
      - name: Commit and Push Changes to Master
        uses: EndBug/add-and-commit@v9
        with:
<<<<<<< HEAD:.github-old/workflows/master-slugs.yml
          author_name: Bart Smeding
          author_email: b@rtsmeding.nl
          committer_name: netdevops-bot
          committer_email: b@rtsmeding.nl
=======
          author_name: NetDevOps-Bot
          author_email: bot@netdevops.it
          committer_name: NetDevOps-Bot
          committer_email: bot@netdevops.it
>>>>>>> 9a0938f68258e00b76df2b39a73763dfa3645c5b:.github/workflows/master-slugs.yml
          default_author: github_actions
          message: "Regenerate master slug list after successful PR merge"
          push: true
